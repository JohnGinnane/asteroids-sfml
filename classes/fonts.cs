using SFML.Graphics;

namespace asteroids {
    public static class Fonts {
        private static Font vectorBattle = new Font("fonts/VectorBattle.ttf");
        public static Font VectorBattle => vectorBattle;

        private static Font hyperspace = new Font("fonts/Hyperspace.ttf");
        public static Font Hyperspace => hyperspace;
    }
}